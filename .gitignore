# OS / OneDrive
desktop.ini
Thumbs.db
.DS_Store

# Dependências (npm) — não publicar no GitHub Pages
node_modules/

# Arquivo unico (gerado, ~8MB) — apenas para abrir/compartilhar localmente
florar-croches-standalone.html
